<% layout('layouts/boilerplate') %>

<h1>Edit Post</h1>
<form action="/posts/<%= post.id %>?_method=PUT" method="POST">
    <div style="display: flex; align-items: start; gap: .5rem;">
        <label for="title">Title</label>
        <input type="text" id="title" name="post[title]" value="<%= post.title %>">

        <label for="description">Description</label>
        <textarea type="text" id="description" name="post[description]" rows="4" cols="40""><%= post.description %></textarea>
    </div>

    <button>Update</button>
<a href=" /posts/<%=post.id %>">Back to post</a>
</form>